# BTC 청산 흐름 전략 v1.0 — 전략 수립 알고리즘 정리

`liquidationstrategyv1preregistration.md` 사전등록 명세를 `liquidation_strategy/`
코드로 옮기며 확정한 알고리즘 설계를 정리한다. 실행 방법이 아니라 **"어떤
로직으로 최적 알고리즘을 도출했는가"**에 초점을 둔다.

---

## 1. 설계 원칙

1. **가설 → 트리거 → 실행**의 3단 구조를 그대로 상태 머신(state machine)으로
   옮긴다. 명세서 문장 하나하나가 코드의 분기 조건 하나에 대응해야 한다 —
   전략 로직과 사전등록 문서가 어긋나면 검증 자체가 무효이기 때문이다.
2. **구조는 고정, 임계값만 탐색.** 진입/청산/트랜치 비율 같은 "실행 구조"는
   코드에 상수로 박아두고, 최적화는 오직 트리거 감지 임계값(예: 청산량
   배수, 반등폭, OI 증가율)에만 적용한다. 이는 명세서 4장의 "캘리브레이션은
   트리거 임계값 조정만 허용" 원칙을 알고리즘 레벨에서 강제한 것이다.
3. **기각 가능성(falsifiability)을 파이프라인에 내장.** 사전 예측(승률·R
   배수 범위)과 기각 조건(표본 수별 최소 기준)을 코드로 표현해, 백테스트가
   끝나면 "채택/기각"이 자동으로 판정되게 한다.

---

## 2. Setup A — 캐스케이드 소진 롱: 상태 머신

```
IDLE ──(T1)──▶ CASCADE ──(T2)──▶ WATCH_EXHAUST ──(T3, ¬T4)──▶ ARMED ──▶ 진입
  ▲                                    │
  └─────────────── 신규 청산 발생 시 되돌림 ──┘
```

- **T1 (캐스케이드 감지)**: `forceOrder` 스트림에서 60초 롤링 윈도우의 SELL
  청산 명목가 합계가 `baseline(24h 시간당 평균) × vol_multiplier` 이상이고,
  건수가 `min_chain` 이상일 때만 IDLE → CASCADE 전이.
- **T2 (가격 이탈)**: `forceOrder`뿐 아니라 **캔들 저가로도** 캐스케이드
  저점을 갱신하며 재평가한다. (구현 중 발견한 버그: forceOrder는 1000ms당
  대표 1건만 오므로, 짧은 캐스케이드는 추가 청산 이벤트 없이 끝나버려
  T2가 영영 평가되지 않는 문제가 있었다 — 캔들 틱에서도 T2를 재확인하도록
  고쳐서 해결했다.)
- **T3 (소진 확인, 진입 게이트)**: 세 조건의 **AND**
  - 마지막 청산 후 `exhaustion_gap_s` 경과, 그 사이 신규 청산 없음
  - 1분 CVD가 음수 → 0 이상으로 전환
  - 저점 대비 `rebound_pct` 이상 반등이 `rebound_hold_s` 동안 유지
  신규 청산이 들어오면 즉시 WATCH_EXHAUST → CASCADE로 되돌아가 반등 타이머를
  리셋한다 — "아직 안 끝났다"는 신호이기 때문.
- **T4 (차단)**: 매크로 이벤트(FOMC/CPI ±30분) 구간이면 T3를 충족해도
  상태를 IDLE로 되돌리고 진입하지 않는다. "강제 흐름"이 아니라 "정보 흐름"일
  가능성을 걸러내는 필터.
- **진입 후**: SL = 저점×(1−0.15%), TP1 = 저점 + 38%되돌림(50% 청산),
  TP2 = 저점 + 61.8%되돌림(잔량), 90분 시간청산. SL에 걸리면 같은
  캐스케이드에 한해 1회 재진입을 허용(`allow_reentry`) — 상태를
  WATCH_EXHAUST로 되돌려 T3를 다시 기다린다.
- 신호가 소비되면(포지션 진입 성공 여부와 무관하게) 탐지 상태를 IDLE로
  리셋해 다음 캐스케이드를 즉시 다시 탐지할 수 있게 한다. 이 리셋을
  빼먹으면 첫 캐스케이드 이후 영구히 다음 캐스케이드를 못 잡는
  버그(실제로 발생했던 버그)가 생긴다.

## 3. Setup B — 트랩드롱 플러시 숏: 상태 머신

```
IDLE ──(박스 상단 돌파)──▶ BREAKOUT ──(T1: OI↑ + 상단 이탈)──▶ TRAP_CONFIRMED
                              │                                      │
                        OI 미확인 시 리셋                    (T2: 재탈환 실패)──▶ 2차 진입
                                                                      │
                                                        (T3: OI 회귀) 연료 소진 → 리셋
```

- **T1 (미끼 국면)**: 4시간 롤링 박스 상단을 캔들 고가가 돌파하고, 그 구간
  OI 증가율이 `oi_increase_pct` 이상이면 "신규 롱 유입"으로 확정. OI 증가가
  없으면 숏커버 랠리로 분류하고 셋업에서 제외한다.
- **T2 (트랩 확정)**: 가격이 박스 상단 아래로 복귀 → 1차 진입(50%). 이후
  `retest_window_s`(재탈환 허용 시간) 내에 상단을 다시 못 넘기면 2차
  진입(나머지 50%). 재탈환에 성공하면 트랩 가설이 틀린 것이므로 셋업을
  취소한다.
- **T3 (연료 확인)**: 트랩 확정 이후에도 유입 OI가 청산되지 않고 남아있는지
  계속 감시한다. OI가 돌파 전 수준으로 회귀하면(이미 다 빠져나감) 연료가
  없다고 보고 셋업을 무효화한다.
- **무효화**: 진입 전 OI가 급감하면(트랩 롱이 자발적으로 탈출) 셋업 취소.
- **진입 후**: SL = sweep high×(1+0.1%), TP1 = 박스 중앙, TP2 =
  `entry×(1−lev_liq_pct)` 중 진입가에 더 가까운 쪽(10x/25x 청산가 근사),
  8시간 시간청산.

## 4. 두 셋업의 상호작용 규칙 (엔진)

`engine.py`가 담당하는, 명세서 3장의 관계:

- **동시 보유 금지**: B 포지션이 열려 있으면 A의 진입 신호는 즉시 체결하지
  않고 보류(`_a_wait_for_b_flat`)했다가, B가 전량 청산된 직후 캔들 틱에서
  진입시킨다.
- **TP2 동적 전환**: B 보유 중 A의 T1(캐스케이드 감지)이 새로 발동하면, 그
  청산 캐스케이드 발생가를 B의 TP2로 즉시 재설정한다. "B가 노리는 청산
  클러스터가 지금 막 터졌다"는 신호이므로 원래의 추정 청산가 대신 실제
  타격 지점을 쓰는 것이 더 정확하기 때문.

## 5. 임계값 캘리브레이션 (그리드서치)

`optimize.py`가 하는 일:

1. 트리거 감지 임계값만 그리드로 나열한다 — Setup A는 `vol_multiplier` ×
   `min_move_pct` × `exhaustion_gap_s` × `rebound_pct` (3⁴=81 조합), Setup B는
   `oi_increase_pct` × `retest_window_s` (3²=9 조합).
2. 각 조합으로 전체 백테스트를 돌려 표본 수·승률·평균 R·평균 기대값(bps)을
   구한다.
3. **목적함수**: 표본이 최소 건수(A=30, B=20) 미만인 조합은 평가에서
   제외한다. 나머지는 `avg_bps`를 최대화하되, 사전 예측 승률 범위(A
   45~60%, B 40~55%)를 벗어나면 벗어난 정도만큼 감점(penalty)한다 — 승률이
   비현실적으로 높게 나오는 조합(과최적화 신호)을 그대로 채택하지 않기
   위한 안전장치.
4. 채택된 파라미터로 최종 통합 백테스트(A+B 동시 운용, 4장의 상호배제 규칙
   적용)를 한 번 더 돌려 대시보드 결과를 만든다.

이 절차 자체가 "최적 알고리즘 도출"의 핵심이다 — 임의로 좋은 파라미터를
고르는 것이 아니라, **탐색 공간을 트리거 임계값으로 한정**하고 **표본 크기
필터 + 사전 예측 범위 페널티**로 과최적화를 억제한 상태에서 기대값을
최대화하는 조합을 고른다.

## 6. 기각 조건 판정 (자동화)

`report.py`의 `rejection_check_a/b`가 명세서 1·2장의 기각 조건을 그대로
코드화한다:

| 셋업 | 규칙 | 판정 |
|---|---|---|
| A | 30건 도달 시 평균 ≤ 0bps | 기각 |
| A | 50건 도달 시 평균 < +8bps 또는 승률 < 35% | 기각 |
| A | 최대 연속 손실 10회 초과 | 기각 |
| B | 20건 도달 시 평균 ≤ 0bps | 기각 |
| B | 40건 도달 시 승률 < 30% 또는 평균 R < 1.2 | 기각 |

표본이 기준 건수에 못 미치면 "표본 미도달"로 표시하고 판정을 보류한다 —
성급한 채택/기각을 막기 위한 장치다.

합성 스트레스 데이터(120일)로 그리드서치 최적값을 적용해 돌린 결과, **Setup
A는 최적 임계값으로도 30건 시점 평균 −22.8bps로 기각 조건을 충족**(v1.0
폐기 대상)했고, **Setup B는 108.1bps/승률 66.7%로 기각 조건 미충족**이었다.
이는 실데이터 검증 이전 파이프라인 자체가 정상적으로 "기각도 가능하다"는
것을 보여준 결과이며, 합성 데이터의 특성에 따른 결과이므로 실데이터
재검증이 필요하다는 점은 변하지 않는다.

## 7. 데이터 계층 분리

알고리즘(트리거·집행·기각판정)과 데이터 소스를 분리해서, 아래 세 가지
실행 경로가 **동일한 엔진 코드**를 공유하도록 설계했다:

- `simulate_data.py` + `backtest.py` — 합성 스트레스 시나리오 (네트워크 불필요)
- `binance_client.py` + `backfill.py` — 실 REST 과거 데이터 (Setup B만, Setup A는
  청산 이력의 공개 REST가 없어 구조적으로 불가능)
- `live_feed.py` — 실 웹소켓 실시간 스트림 (섀도 단계, 명세서 5장 1단계)

세 경로 모두 `report.py`가 동일한 JSON 스키마로 변환하므로, 대시보드
(`dashboard_template.html`)는 데이터 출처를 몰라도 동작한다.

## 8. Setup C — OU 평균회귀 (A/B와 다른 아키텍처)

사전등록: `uploads/strategy_c_ou_reversion_spec.md`. Setup A/B는 실시간
스트리밍 이벤트(ForceOrder/Candle/OI)를 하나씩 받아 내부 상태를 갱신하는
상태 머신이지만, Setup C는 그 방식이 맞지 않는다 — VR(Variance Ratio)·
AR(1) 반감기 추정이 매 봉마다 최근 24h(288봉) 구간 전체를 다시 계산해야
하는 배치성 통계라, 스트리밍 상태 누적보다 "5분봉 DataFrame을 통째로
받아 봉 인덱스 i를 훑는" 순수 함수 쪽이 훨씬 단순하고 정확하다.

그래서 구조 자체를 다르게 가져갔다:

- `setup_c.py` — `evaluate_bar(df, i, p)`(진입 게이트 G1~G4/F1~F2 판정),
  `conditions(df, i, p)`(첫 실패에서 멈추지 않고 G1~G4를 항상 전부 계산 —
  GUI 체크리스트용, setup_a/b.py의 conditions()와 동일 역할), `should_exit`
  (TP/SL/시간청산)만 있는 상태 없는 함수 집합. 포지션 자체를 들고 있지 않는다.
- `backtest_c.py` — 재진입 횟수·쿨다운처럼 "지금 포지션이 어떤 상태인가"를
  들고 있는 `Ledger`(방향별 독립: `ledger_c_long`/`ledger_c_short`)가 이걸
  감싼다. `compare_toggles()`가 명세 7장의 F1/F2 on/off 4조합 + VR 게이트
  on/off 비교를 한 번에 돌려준다. 과거 데이터로 미리 검증할 때 쓴다.
- `live_setup_c.py` — `LiveSetupCRunner`가 확정 5분봉을 하나씩 받아
  `backtest_c.Ledger`를 그대로 재사용해 실시간으로 구동한다. 라이브
  모드는 바이낸스가 이미 주는 5분봉을 그대로 받고, 합성 모드는
  `MinuteAggregator`로 1분봉을 5분봉으로 직접 집계해 같은 인터페이스로
  공급한다(표시용 집계와 같은 방식, 별도 구현).
- `report_c.py` — A/B의 `report.py`와 판정 기준이 근본적으로 달라
  (BE-WR 대비 실측 승률, 반감기 대비 보유시간, VR게이트 기여도, A/B와의
  트레이드 상관) 공유하지 않고 별도 모듈로 뒀다.

**`StrategyEngine` 자체에는 여전히 연결하지 않았지만(엔진 코드 무수정
원칙 유지), GUI·라이브/합성 실행 경로에는 A/B와 동일한 수준으로 연동돼
있다** — Dashboard의 트리거 조건 체크리스트, 포지션·PnL 테이블, Log
탭, Settings의 파라미터 편집·중지·즉시재시작까지 전부 A/B와 나란히
동작한다(단, 실주문은 A/B와 동일하게 비활성 — 페이퍼/섀도 전용). 이는
A/B가 라이브에 연결되기 전 거쳤던 것과 같은 절차를 그대로 밟는 것이다 —
라이브 섀도로 표본을 쌓되, 최소 표본(방향별 30건) 도달 전까지는
`report_c.py`의 기각조건 판정이 "표본 미도달"로 보류된다. 명세서 6장
기각조건 4개(BE-WR 대비 기댓값, 반감기 대비 보유시간 과다, VR게이트
무기여, Setup A/B와 상관 과다) 중 하나라도 걸리면 폐기 대상이다.

---

## 관련 코드

| 알고리즘 요소 | 파일 |
|---|---|
| Setup A 상태 머신 | `liquidation_strategy/setup_a.py` |
| Setup B 상태 머신 | `liquidation_strategy/setup_b.py` |
| 상호배제/TP2 동적전환 엔진 | `liquidation_strategy/engine.py` |
| 임계값 그리드서치 | `liquidation_strategy/optimize.py` |
| 기각조건 판정 + 리포트 스키마 | `liquidation_strategy/report.py` |
| 합성 데이터 생성 | `liquidation_strategy/simulate_data.py` |
| 실데이터 REST/실시간 어댑터 | `liquidation_strategy/binance_client.py`, `live_feed.py`, `backfill.py` |
| Setup C 신호/청산 순수함수 | `liquidation_strategy/setup_c.py` |
| Setup C 독립 원장 백테스트 | `liquidation_strategy/backtest_c.py` |
| Setup C 실시간 러너(GUI 연동) | `liquidation_strategy/live_setup_c.py` |
| Setup C 기각조건 판정 | `liquidation_strategy/report_c.py` |
